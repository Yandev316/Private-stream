Place your background image here and name it `bg.jpg`.

Recommended size: at least 1920x1080. The CSS uses `/img/bg.jpg` as the background image.

If you want a different filename, update `public/css/style.css` background URL in the `body::before` rule.
